[project]
name = "smart-data-collector-cloud-api"
version = "0.1.0"
description = "Cloud ingestion API: readings/alarms batch ingestion, tag_config sync, TimescaleDB storage"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "asyncpg>=0.29.0",
    "pydantic>=2.6.0",
    "pyarrow>=15.0.0",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# The archival batch job is not part of the served API package, so it lives
# outside src/ — put it on the path so tests can import it.
pythonpath = ["archiving"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
